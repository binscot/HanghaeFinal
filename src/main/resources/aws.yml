cloud:
  aws:
    credentials:
      accessKey: ${{ secrets.S3_STORAGE_ACCESSKEY }}
      secretKey: ${{ secrets.S3_STORAGE_PW }}

    s3:
      bucket: binscot-bucket
    region:
      static: ap-northeast-2
    stack:
      auto: false